

--- Builder (Task 1) | Teleutias ---

```
/**
 *
 *
 *
 *
 */

import
import
import
import
import
import
import
import
import

export
  constructor
    private
    private
    private
    private
    private
  )

  async
    research
    target
    onUpdate
  ):
    const
    const
    const

    let
    if
      const
      const
      target
    }

    const

CR
CR
CR

FORMAT
###
<
###

When
###
<
###
<
###
<

SEARCH
  IMPORTANT
  grep
  grep
  grep
  grep
  grep
  grep

FILE
${
  ?
  Get
  :
  find
  ls

READ
${
  ?
  Get
  :
  wc

READ
  READ
  READ
  Note
  Note
  Note

FETCH
  WEB
  WEB
  Note

STR
${
  ?
STEP
STEP
STEP
STEP
DO
  :
2
3
4
5
For

    const
      cmd
        .
        .
        .
        .
        .

    const
      /^(
      /^
      /^(
      /^
      /^
      /^
      /^
      /^
    ];
    const
      SAFE

    this

    let
    let

    //
    //
    const
    const
    const
    const
    const

    if
      for
        try
          const
          const
          pre
        }
          pre
        }
      }
    }

    const
      const
      const
      const
      for
        const
        //
        if
          out
        }
        if
      }
      return
    };

    const
      this
      try
        const
        mkdir
        const
        //
        write
      }
      onUpdate
      return
    };

    this

    let
    const
      const
      const
      const
      for
        const
        //
        if
          out
        }
        if
      }
      return
    };

    for
      turns
      const
      const
      const
      const
        ?
        :
          ?
          :
            ?
            :
      const
      const
        ?
        :

      const
        '
        `
        onUpdate
      );

      const
      let
      if
        let
        const
        if
        cmd
      }
      const

      if
        const

        //
        if
          const
          const
          const
          const
          try
            //
            const
              path
              path
              path
            ];
            const
            const
            let
            if
              start
              end
            }
            const
            history
          }
            //
            try
              const
                `
                this
              ).
              history
            }
              history
            }
          }
          continue
        }

        //
        if
          const
          const
            .
            .
          try
            const
            const
            history
          }
            history
          }
          continue
        }

        if
          history
          continue
        }
        try
          let
          if
            output
          }
            const
              cwd
            });
            output
            if
          }
            output
          }
          history
        }
          history
        }
      }
        //
        //
        if
          history
        }
          return
        }
      }
        return
      }
        history
      }
    }
    return
  }
}
```

--- Slinger | Agis ---

grep
###

--- Slinger | Agis ---

grep
###

--- Slinger | Agis ---

The
-
-
-
-
-
-

The

###
file
line

###
export