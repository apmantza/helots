# Slinger Report
**Task:** Read the file C:\Users\R3LiC\Desktop\helots-pi\src\adapters\mcp-server.ts and show me its full content, especially how the engine is initialized and started.

### SUMMARY
The file `C:\Users\R3LiC\Desktop\helots-pi\src\adapters\mcp-server.ts` has been fully analyzed.

**Engine Initialization:**
- The `HelotEngine` is instantiated at the module level (top of the file) using a `config` object.
- **Line 43:** `const engine = new HelotEngine(config);`
- The `config` object (lines 23-33) is constructed from environment variables with sensible defaults (LLM URLs, API keys, model names, state directory).
- It also attempts to load an optional override from `.helots/config.json` (lines 36-42).

**Engine Start/Execution:**
- The engine is **not** started directly in this file. Instead, it is initialized as a singleton instance.
- The actual execution of engine methods happens inside the `CallToolRequestSchema` handler (lines 178-223) when specific MCP tools are called:
  - `helot_slinger`: Calls `engine.executeSlinger(...)` (Line 192)
  - `helot_run`: Calls `engine.executeHelots(...)` (Line 206)
  - `helot_hoplite`: Calls `engine.executeHoplite(...)` (Line 218)
- The server itself starts via the `main()` function (lines 235-243), which connects to a `StdioServerTransport`. This is the standard entry point for an MCP server, not the engine itself.

**Key Findings:**
1.  **Initialization:** `const engine = new HelotEngine(config);` (Line 43)
2.  **Execution Trigger:** Tool calls via MCP (`helot_slinger`, `helot_run`, `helot_hoplite`) trigger engine methods.
3.  **Server Startup:** `main()` function (Line 235) starts the MCP transport, not the engine directly.

### LOCATIONS
- C:\Users\R3LiC\Desktop\helots-pi\src\adapters\mcp-server.ts:43 (Engine instantiation)
- C:\Users\R3LiC\Desktop\helots-pi\src\adapters\mcp-server.ts:192 (Slinger execution)
- C:\Users\R3LiC\Desktop\helots-pi\src\adapters\mcp-server.ts:206 (Helots execution)
- C:\Users\R3LiC\Desktop\helots-pi\src\adapters\mcp-server.ts:218 (Hoplite execution)
- C:\Users\R3LiC\Desktop\helots-pi\src\adapters\mcp-server.ts:235 (MCP Server startup)

### EVIDENCE
```typescript
// Engine Initialization (Line 43)
const engine = new HelotEngine(config);

// Execution inside Tool Handlers (Lines 192, 206, 218)
if (name === "helot_slinger") {
    // ...
    const result = await engine.executeSlinger(researchTask, targetFiles, (data: { text: string }) => {
        // ...
    });
}

if (name === "helot_run") {
    // ...
    const result = await engine.executeHelots(taskSummary, implementationPlan, (data) => {
        // ...
    }, frontierTasks);
}

if (name === "helot_hoplite") {
    // ...
    const result = await engine.executeHoplite(file, instruction, (data: { text: string }) => {
        // ...
    });
}

// Server Startup (Lines 235-243)
async function main() {
    const transport = new StdioServerTransport();
    await server.connect(transport);
    console.error("Helots MCP Server running on stdio");
}

main().catch((error) => {
    // ...
});
```