# Slinger Report
**Task:** In C:/Users/R3LiC/Desktop/helots-pi/src/core/llama-client.ts, show me:
1. The `streamCompletion` function signature (exact parameter names and types)
2. How `profile` (string key) is resolved to actual sampling params (max_tokens, temperature, etc.)
3. Where max_tokens is used in the API call payload

Also check model-registry.ts: what does the profile object look like for INSTRUCT_CODE? What are the fields?

### SUMMARY
**1. `streamCompletion` Function Signature**
Located in `C:/Users/R3LiC/Desktop/helots-pi/src/core/llama-client.ts` (lines 115-123):
```typescript
async streamCompletion(
    messages: { role: string; content: string }[],
    role: TaskRole,
    profileKey: string,
    onChunk: (chunk: string, metrics: { genTps: number; promptEvalTps: number; isFirstToken: boolean; promptTokens: number; genTokens: number; maxTokens: number }) => void,
    onEnd: () => void
): Promise<void>
```

**2. Profile Resolution Logic**
Located in `C:/Users/R3LiC/Desktop/helots-pi/src/core/llama-client.ts` (lines 154-157):
- The function calls `getProfilesForModel(targetModel)` to retrieve the registry for the specific model.
- It attempts to access the profile by the provided `profileKey`: `profiles[profileKey]`.
- If the key is missing, it defaults to `'THINKING_GENERAL'`.

**3. `max_tokens` Usage in API Payload**
Located in `C:/Users/R3LiC/Desktop/helots-pi/src/core/llama-client.ts` (line 188):
- `max_tokens` is extracted directly from the resolved profile object: `max_tokens: profile.max_tokens`.
- It is included in the `requestBody` sent to `/v1/chat/completions`.

**4. `INSTRUCT_CODE` Profile Object Structure**
Located in `C:/Users/R3LiC/Desktop/helots-pi/src/core/model-registry.ts` (lines 28-29 for Qwen 27B, lines 50-51 for Qwen 35B MoE):
- **Fields**: `temperature`, `top_p`, `top_k`, `min_p`, `presence_penalty`, `repetition_penalty`, `enableThinking`, `max_tokens`, `extra_body`.
- **Specific Values for Qwen Models**:
  - `temperature`: 0.4
  - `top_p`: 0.9
  - `top_k`: 20
  - `max_tokens`: 8192
  - `enableThinking`: false
  - `extra_body`: `{ chat_template_kwargs: { enable_thinking: false } }`

### LOCATIONS
- `C:/Users/R3LiC/Desktop/helots-pi/src/core/llama-client.ts`:115-123 (Signature)
- `C:/Users/R3LiC/Desktop/helots-pi/src/core/llama-client.ts`:154-157 (Resolution)
- `C:/Users/R3LiC/Desktop/helots-pi/src/core/llama-client.ts`:188 (max_tokens usage)
- `C:/Users/R3LiC/Desktop/helots-pi/src/core/model-registry.ts`:28-29, 50-51 (INSTRUCT_CODE definition)

### EVIDENCE
```typescript
// llama-client.ts: Signature
async streamCompletion(
    messages: { role: string; content: string }[],
    role: TaskRole,
    profileKey: string,
    onChunk: (chunk: string, metrics: { genTps: number; promptEvalTps: number; isFirstToken: boolean; promptTokens: number; genTokens: number; maxTokens: number }) => void,
    onEnd: () => void
): Promise<void> {

// llama-client.ts: Resolution
const profiles = getProfilesForModel(targetModel);
let profile = profiles[profileKey] || profiles['THINKING_GENERAL'];

// llama-client.ts: Payload
const requestBody = {
    // ...
    max_tokens: profile.max_tokens,
    // ...
};

// model-registry.ts: INSTRUCT_CODE definition
INSTRUCT_CODE: { temperature: 0.4, top_p: 0.9, top_k: 20, min_p: 0.0, presence_penalty: 0.0, repetition_penalty: 1.0, enableThinking: false, max_tokens: 8192, extra_body: { chat_template_kwargs: { enable_thinking: false } } },
```