# Slinger Report
**Task:** Check what HELOT_STATE_DIR is set to in the Claude settings or MCP config. Then list the contents of .helot-mcp-connector/runs/ directory. Also check src/adapters/dashboard-server.ts — what is the stateDir value passed to startDashboard, and how does /api/runs resolve the runsDir path?

### SUMMARY
The `HELOT_STATE_DIR` environment variable is used throughout the codebase to configure the state directory path. When not set, it defaults to `.helot-state` in `index.ts` and `.helots` in the MCP adapters. The `.helot-mcp-connector/runs/` directory contains one run folder (`run-1772527493898-y4cb697`) with context, progress, review, and trace files. In `dashboard-server.ts`, the `startDashboard` function accepts a `stateDir` parameter which is passed to `SpartaRecordManager`. The `/api/runs` endpoint resolves the `runsDir` by joining the `stateDir` with the `'runs'` subdirectory (`path.join(stateDir, 'runs')`).

### LOCATIONS
- `C:\Users\R3LiC\Desktop\helots-pi\index.ts:27`: Default `HELOT_STATE_DIR` value
- `C:\Users\R3LiC\Desktop\helots-pi\src\adapters\mcp-server.ts:27`: MCP server default `HELOT_STATE_DIR`
- `C:\Users\R3LiC\Desktop\helots-pi\src\adapters\mcp.ts:29`: MCP client default `HELOT_STATE_DIR`
- `C:\Users\R3LiC\Desktop\helots-pi\src\adapters\pi.ts:19`: Pi adapter default `HELOT_STATE_DIR`
- `C:\Users\R3LiC\Desktop\helots-pi\.helot-mcp-connector\runs\`: Directory contents listing
- `C:\Users\R3LiC\Desktop\helots-pi\src\adapters\dashboard-server.ts:44`: `startDashboard` function signature
- `C:\Users\R3LiC\Desktop\helots-pi\src\adapters\dashboard-server.ts:107`: `runsDir` path resolution

### EVIDENCE
```typescript
// index.ts:27
stateDir: process.env.HELOT_STATE_DIR || ".helot-state",

// src/adapters/mcp-server.ts:27
stateDir: process.env.HELOT_STATE_DIR || ".helots",

// src/adapters/dashboard-server.ts:44
export function startDashboard(engine: HelotEngine, stateDir: string, port = 7771): void {

// src/adapters/dashboard-server.ts:107-110
const runsDir = path.join(stateDir, 'runs');
if (!fs.existsSync(runsDir)) { res.writeHead(200, { 'Content-Type': 'application/json' }); res.end('[]'); return; }
const runs = fs.readdirSync(runsDir)
  .filter(name => fs.statSync(path.join(runsDir, name)).isDirectory())
```