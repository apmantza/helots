# Slinger Report
**Task:** Read src/eba_benchmarking/data/solvency.py. Show: (1) all imports, (2) how fetch_facts_oth is called (arguments, usage), (3) how group_weighted_avg is called (arguments, usage). Include the actual code snippets for each usage.

### SUMMARY
Based on the pre-loaded content of `src/eba_benchmarking/data/solvency.py`:

1.  **Imports**:
    *   `pandas as pd`
    *   `sqlite3`
    *   `streamlit as st`
    *   `os`
    *   `DB_NAME`, `SOLVENCY_ITEMS` from `..config`
    *   `MIN_PERIOD`, `get_benchmark_leis` from `.base`
    *   `fetch_facts_oth`, `group_weighted_avg` from `.data_utils`

2.  **`fetch_facts_oth` Usage**:
    *   Called in `get_solvency_kpis` (line 22) and `get_solvency_averages` (line 83).
    *   Arguments: `(conn, leis_str, items_str, MIN_PERIOD)`.
    *   Usage: Fetches raw fact data for specific LEIs and item IDs to calculate solvency KPIs.

3.  **`group_weighted_avg` Usage**:
    *   Called in `get_solvency_averages` within the `get_pivoted_data` function.
    *   Arguments: `(group, column_name, weight_column)`.
    *   Usage: Calculates weighted averages for ratios (e.g., CET1 Ratio, AT1 Ratio, RWA Density) across a group of banks using a specific weight column (e.g., TREA, total_assets).

### LOCATIONS
*   **Imports**: Line 1-7
*   **fetch_facts_oth**: Line 22, Line 83
*   **group_weighted_avg**: Line 103, Line 104, Line 105, Line 109, Line 110, Line 114, Line 115, Line 119, Line 120

### EVIDENCE

**Imports:**
```python
import pandas as pd
import sqlite3
import streamlit as st
import os
from ..config import DB_NAME, SOLVENCY_ITEMS
from .base import MIN_PERIOD, get_benchmark_leis
from .data_utils import fetch_facts_oth, group_weighted_avg
```

**fetch_facts_oth Usage:**
```python
# In get_solvency_kpis
df = fetch_facts_oth(conn, leis_str, items_str, MIN_PERIOD)

# In get_solvency_averages (inside get_pivoted_data)
df_raw = fetch_facts_oth(conn, leis_sql, items_str_ext, MIN_PERIOD)
```

**group_weighted_avg Usage:**
```python
# In get_solvency_averages (inside get_pivoted_data)
' CET1 Ratio': group_weighted_avg(group, 'CET1 Ratio', 'TREA'),
'Total Capital Ratio': group_weighted_avg(group, 'Total Capital Ratio', 'TREA'),
'Leverage Ratio': group_weighted_avg(group, 'Leverage Ratio', 'TREA'),

# In get_solvency_averages (inside get_pivoted_data)
df_f['AT1 Ratio (calc)'] = df_f.apply(lambda x: group_weighted_avg(df_banks, 'AT1_R', 'TREA'), axis=1)
df_f['Tier 2 Ratio (calc)'] = df_f.apply(lambda x: group_weighted_avg(df_banks, 'T2_R', 'TREA'), axis=1)
df_f['RWA Density'] = df_f.apply(lambda x: group_weighted_avg(df_banks, 'RWA_Den', 'total_assets'), axis=1)
```