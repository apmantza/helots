# Slinger Report
**Task:** In C:/Users/R3LiC/Desktop/helots-pi/src/core/engine.ts, find psiloiMetrics object: what fields does it have, how is it updated, and where is it used? Show the full object structure.

### SUMMARY
The `psiloiMetrics` object is a metrics collector for tracking token usage across the three main subagents (Scout, Builder, Peltast) in the Helot Engine.

**Object Structure:**
It is a plain JavaScript object with three properties, each containing an object with three fields:
1.  `scout`: `{ in: number, out: number, tps: number }`
2.  `builder`: `{ in: number, out: number, tps: number }`
3.  `peltast`: `{ in: number, out: number, tps: number }`

*Note: The `scout` metrics are initialized but the `scout` subagent is not currently passed to `runSubagent` in the provided code (the `runSubagent` calls for Scout are missing or use a different pattern in this snippet, though `psiloiMetrics.scout` is initialized).*

**Initialization:**
It is initialized as an empty object with zero values for all fields at the start of the orchestration flow:
-   Line 886: `const psiloiMetrics = { scout: { in: 0, out: 0, tps: 0 }, builder: { in: 0, out: 0, tps: 0 }, peltast: { in: 0, out: 0, tps: 0 } };`
-   Line 1440: Same initialization in `executeApprovedTasks`.

**Usage/Updates:**
1.  **Passed to Subagents:** The object is passed as the `metrics` argument to `runSubagent` for the Builder and Peltast.
    -   Line 393: `psiloiMetrics.builder` is passed to `runSubagent` for the Builder role.
    -   Line 660: `psiloiMetrics.peltast` is passed to `runSubagent` for the Peltast role.
2.  **Internal Update:** The `runSubagent` method updates the specific subagent's metrics object in-place. It sets `promptTokens`, `genTokens`, and `genTps` on the passed metrics object (e.g., `finalMetrics = m` where `m` contains the updated counts).
3.  **Execution Flow:**
    -   In `executeHelots` (Line 886), it is created and passed to `runTaskLoop`.
    -   In `runTaskLoop` (Line 824), it is passed to `runOneTask`.
    -   In `runOneTask` (Line 218), it is passed to `runSubagent` for Builder and Peltast.
    -   In `executeApprovedTasks` (Line 1440), it is re-initialized and passed to `runTaskLoop`.

### LOCATIONS
-   **C:/Users/R3LiC/Desktop/helots-pi/src/core/engine.ts:886** - Initialization of `psiloiMetrics` in `executeHelots`.
-   **C:/Users/R3LiC/Desktop/helots-pi/src/core/engine.ts:1440** - Initialization of `psiloiMetrics` in `executeApprovedTasks`.
-   **C:/Users/R3LiC/Desktop/helots-pi/src/core/engine.ts:218** - Function signature for `runOneTask` accepting `psiloiMetrics`.
-   **C:/Users/R3LiC/Desktop/helots-pi/src/core/engine.ts:393** - Passing `psiloiMetrics.builder` to `runSubagent`.
-   **C:/Users/R3LiC/Desktop/helots-pi/src/core/engine.ts:660** - Passing `psiloiMetrics.peltast` to `runSubagent`.
-   **C:/Users/R3LiC/Desktop/helots-pi/src/core/engine.ts:824** - Function signature for `runTaskLoop` accepting `psiloiMetrics`.

### EVIDENCE
```typescript
// Initialization (Line 886 & 1440)
const psiloiMetrics = { scout: { in: 0, out: 0, tps: 0 }, builder: { in: 0, out: 0, tps: 0 }, peltast: { in: 0, out: 0, tps: 0 } };

// Function signature (Line 218)
private async runOneTask(
  task: HelotTask,
  runId: string,
  modelName: string,
  globalContext: string,
  implementationPlan: string,
  psiloiMetrics: { builder: any; peltast: any },
  reviewFile: string,
  onUpdate: ((data: any) => void) | undefined,
  allTasks: HelotTask[] = []
): Promise<{ passed: boolean; escalation?: string }> {

// Passing to Builder (Line 393)
const builderRaw = await this.runSubagent(
  "Builder", builder.name, builderSystem,
  `Mission ID: ${runId}\nTask: ${task.description}`,
  onUpdate, psiloiMetrics.builder, builderProfile, modelName, undefined, builderMaxTokensOverride
);

// Passing to Peltast (Line 660)
peltastOut = await this.runSubagent(
  "Peltast", peltast.name, peltastSystem,
  `Verify task completion: ${task.description}`,
  onUpdate, psiloiMetrics.peltast, "PELTAST", modelName
);
```