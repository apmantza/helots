# Slinger Report
**Task:** In src/core/engine.ts, find every call site of `runTaskLoop` — show the exact line numbers and the full call expression for each.

### SUMMARY
Found 2 call sites for `runTaskLoop` in `src/core/engine.ts`. Both are `await` calls on `this.runTaskLoop` with identical argument structures, located at lines 1113 and 1476. The definition is at line 823.

### LOCATIONS
src/core/engine.ts:1113
src/core/engine.ts:1476

### EVIDENCE
src/core/engine.ts:1113:    const halt = await this.runTaskLoop(taskNodes, runId, modelName, globalContext, cappedPlan, psiloiMetrics, reviewFile, onUpdate);
src/core/engine.ts:1476:    const halt = await this.runTaskLoop(taskNodes, runId, modelName, globalContext, tasks, psiloiMetrics, reviewFile, onUpdate);