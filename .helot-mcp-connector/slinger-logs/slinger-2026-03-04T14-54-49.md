# Slinger Report
**Task:** In src/adapters/dashboard-server.ts, show me the /api/stats handler — specifically how localTokens is accumulated. Show the exact lines around the subagent_done handling.

### SUMMARY
In `src/adapters/dashboard-server.ts`, the `/api/stats` handler accumulates `localTokens` by reading the `events.jsonl` file. For every event with type `subagent_done`, it adds the `promptTokens` to `localTokens.in`, `genTokens` to `localTokens.out`, and updates `tps` if `ev.tps` is greater than 0.

### LOCATIONS
- `C:\Users\R3LiC\Desktop\helots-pi\src\adapters\dashboard-server.ts:88-94`

### EVIDENCE
```typescript
if (ev.type === 'subagent_done') {
  localTokens.in += ev.promptTokens || 0;
  localTokens.out += ev.genTokens || 0;
  if (ev.tps > 0) { localTokens.tps = ev.tps; }
}
```