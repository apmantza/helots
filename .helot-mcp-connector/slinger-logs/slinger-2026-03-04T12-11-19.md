# Slinger Report
**Task:** In C:/Users/R3LiC/Desktop/helots-pi/src/core/engine.ts, find:
1. Where maxTokens from getProps() is stored (variable name, line number)
2. Where streamCompletion is called for the builder (the actual call, not the definition)
3. Where the builderProfile / profile object is passed to streamCompletion or runSubagent
4. Whether there's a `profile` object spread/override pattern near the builder call

Focus on lines 280-450 of the file. Report exact line numbers and code snippets.

### SUMMARY
Based on the pre-loaded file content and command history:

1.  **`maxTokens` Storage**: The `maxTokens` value returned from `getProps()` is stored in a variable named `maxTokens` (line 838 and 1073). It is immediately used to calculate `availableChars` and various caps (`MANIFEST_CAP`, `PLAN_CAP`, `SLINGER_CAP`, `FILE_CAP`).

2.  **`streamCompletion` Call**: The actual call to `streamCompletion` occurs at **line 1505** within the `runSubagent` method (or a similar internal method). The call signature is:
    `await this.client.streamCompletion([{ role: 'system', content: systemPrompt }, { role: 'user', content: userPrompt }], role as TaskRole, profile, (chunk, m) => { ...`

3.  **`builderProfile` / `profile` Object**:
    *   In the `runOneTask` method (lines ~330-340 in the pre-loaded content), the variable `builderProfile` is defined as a string (`"INSTRUCT_CODE"` or `"THINKING_CODE"`).
    *   This `builderProfile` string is passed as the 6th argument to `this.runSubagent` (line ~338).
    *   Inside `runSubagent` (inferred from the `streamCompletion` call at line 1505), this argument is mapped to the `profile` parameter used in the `streamCompletion` call.
    *   There is **no** `profile` object spread/override pattern near the builder call. The profile is passed as a simple string identifier.

### LOCATIONS
*   **maxTokens variable**: `C:/Users/R3LiC/Desktop/helots-pi/src/core/engine.ts:838`, `C:/Users/R3LiC/Desktop/helots-pi/src/core/engine.ts:1073`
*   **streamCompletion call**: `C:/Users/R3LiC/Desktop/helots-pi/src/core/engine.ts:1505`
*   **builderProfile definition**: `C:/Users/R3LiC/Desktop/helots-pi/src/core/engine.ts:336` (approximate, based on context)
*   **builderProfile passed to runSubagent**: `C:/Users/R3LiC/Desktop/helots-pi/src/core/engine.ts:338` (approximate)

### EVIDENCE
```typescript
// Line 838: maxTokens extracted from getProps()
const { modelName, maxTokens } = await this.client.getProps();

// Line 848: maxTokens used for calculation
const availableChars = Math.floor(maxTokens * (1 - RESERVED_FRACTION) * CHARS_PER_TOKEN);

// Lines ~336-338: builderProfile definition and pass
const builderProfile = (isSurgical || !isCreateTask) ? "INSTRUCT_CODE" : "THINKING_CODE";
const builderRaw = await this.runSubagent(
  "Builder", builder.name, builderSystem,
  `Mission ID: ${runId}\nTask: ${task.description}`,
  onUpdate, psiloiMetrics.builder, builderProfile, modelName
);

// Line 1505: streamCompletion call with profile argument
await this.client.streamCompletion([{ role: 'system', content: systemPrompt }, { role: 'user', content: userPrompt }], role as TaskRole, profile, (chunk, m) => {
```