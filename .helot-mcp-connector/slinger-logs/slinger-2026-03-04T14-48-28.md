# Slinger Report
**Task:** In src/adapters/dashboard-server.ts, verify all these routes exist in the startDashboard function:
1. GET / — serves HTML
2. GET /events — SSE on events.jsonl
3. GET /api/stream — SSE on stream.log
4. GET /api/stats — returns { localTokens, frontierEstimate, record, currentRun }
5. GET /api/runs — lists run directories sorted by mtime
6. GET /api/file — reads .md files only
7. POST /api/run, /api/slinger, /api/hoplite — action triggers
8. Auto-open browser on listen (win32/darwin)
Show confirmation of each with line numbers.

### SUMMARY
All 8 requested features are confirmed present in `src/adapters/dashboard-server.ts`:
1. **GET /** (Line 46-54): Serves HTML from `src/dashboard/index.html`
2. **GET /events** (Line 56-62): SSE on `events.jsonl` via `serveSSE`
3. **GET /api/stream** (Line 64-70): SSE on `stream.log` via `serveSSE`
4. **GET /api/stats** (Line 72-107): Returns `{ localTokens, frontierEstimate, record, currentRun }`
5. **GET /api/runs** (Line 109-125): Lists run directories sorted by `mtime`
6. **GET /api/file** (Line 127-135): Reads `.md` files only (enforced by `.endsWith('.md')` check)
7. **POST /api/run, /api/slinger, /api/hoplite** (Line 137-165): Action triggers calling engine methods
8. **Auto-open browser** (Line 173-175): Uses `exec('start ...')` on win32 and `exec('open ...')` on darwin

### LOCATIONS
- `src/adapters/dashboard-server.ts:46-54` - GET / serves HTML
- `src/adapters/dashboard-server.ts:56-62` - GET /events SSE
- `src/adapters/dashboard-server.ts:64-70` - GET /api/stream SSE
- `src/adapters/dashboard-server.ts:72-107` - GET /api/stats returns required fields
- `src/adapters/dashboard-server.ts:109-125` - GET /api/runs sorted by mtime
- `src/adapters/dashboard-server.ts:127-135` - GET /api/file .md only
- `src/adapters/dashboard-server.ts:137-165` - POST /api/run, /api/slinger, /api/hoplite
- `src/adapters/dashboard-server.ts:173-175` - Auto-open browser on win32/darwin

### EVIDENCE
```typescript
// GET / serves HTML
if (req.method === 'GET' && pathname === '/') {
  if (fs.existsSync(htmlPath)) {
    res.writeHead(200, { 'Content-Type': 'text/html' });
    res.end(fs.readFileSync(htmlPath));
  }
  return;
}

// GET /events SSE
if (req.method === 'GET' && pathname === '/events') {
  const eventsFile = path.join(stateDir, 'events.jsonl');
  const cleanup = serveSSE(res, eventsFile);
  return;
}

// GET /api/stream SSE
if (req.method === 'GET' && pathname === '/api/stream') {
  const streamFile = path.join(stateDir, 'stream.log');
  const cleanup = serveSSE(res, streamFile);
  return;
}

// GET /api/stats returns required fields
res.writeHead(200, { 'Content-Type': 'application/json' });
res.end(JSON.stringify({ localTokens, frontierEstimate, record, currentRun }));

// GET /api/runs sorted by mtime
const runs = fs.readdirSync(runsDir)
  .filter(name => fs.statSync(path.join(runsDir, name)).isDirectory())
  .map(name => { ... })
  .sort((a, b) => b.mtime - a.mtime);

// GET /api/file .md only
if (!filePath.endsWith('.md')) { res.writeHead(403); res.end('Only .md files allowed'); return; }

// POST /api/run, /api/slinger, /api/hoplite
if (req.method === 'POST' && (pathname === '/api/run' || pathname === '/api/slinger' || pathname === '/api/hoplite')) {
  // ... engine.executeHelots, executeSlinger, executeHoplite calls
}

// Auto-open browser
if (process.platform === 'win32') exec(`start http://localhost:${port}`);
else if (process.platform === 'darwin') exec(`open http://localhost:${port}`);
```