# Slinger Report
**Task:** In C:/Users/R3LiC/Desktop/helots-pi/src/adapters/mcp-server.ts, show me:
1. How helot_run and helot_slinger tools are defined (their inputSchema, the handler pattern)
2. How they call into the engine (what engine methods they invoke)
3. The overall structure — is there a tools array, a switch statement, etc.?

I need enough context to add a new helot_hoplite tool following the same pattern.

### SUMMARY
Based on the pre-loaded file `C:/Users/R3LiC/Desktop/helots-pi/src/adapters/mcp-server.ts`, here is the analysis for adding a new `helot_hoplite` tool:

**1. Tool Definition Structure**
Tools are defined in a constant array `const TOOLS: Tool[]`. Each tool object contains:
*   `name`: The tool identifier (e.g., `"helot_slinger"`, `"helot_run"`).
*   `description`: A human-readable string explaining the tool's purpose.
*   `inputSchema`: A JSON Schema object describing the expected arguments.
    *   `type`: Always `"object"`.
    *   `properties`: Defines argument names, types, and descriptions.
    *   `required`: An array of argument names that must be present.

**2. Handler Pattern & Engine Invocation**
The server uses a `server.setRequestHandler(CallToolRequestSchema)` to handle incoming tool calls.
*   **Pattern**: It extracts `name` and `arguments` from `request.params`.
*   **Logic**: It uses `if (name === "tool_name")` blocks to route to specific logic.
*   **Engine Methods**:
    *   `helot_slinger` calls `engine.executeSlinger(researchTask, targetFiles, progressCallback)`.
    *   `helot_run` calls `engine.executeHelots(taskSummary, implementationPlan, progressCallback, frontierTasks)`.
*   **Progress Callback**: Both methods accept a callback function `(data: { text: string }) => { ... }` to handle streaming updates.
*   **Return Format**: The handler returns an object with `content: [{ type: "text", text: result }]`. Errors are caught and returned with `isError: true`.

**3. Overall Structure**
*   **Tools Array**: Defined at the top level (`const TOOLS`).
*   **List Handler**: `server.setRequestHandler(ListToolsRequestSchema, ...)` returns the `TOOLS` array.
*   **Call Handler**: `server.setRequestHandler(CallToolRequestSchema, ...)` contains the logic for specific tool execution.
*   **Switch/If**: The code uses sequential `if` statements rather than a `switch` statement to check the tool name.

**To add `helot_hoplite`**:
1.  Add a new object to the `TOOLS` array with `name: "helot_hoplite"`.
2.  Define its `inputSchema` properties.
3.  Add a new `if (name === "helot_hoplite")` block inside the `CallToolRequestSchema` handler.
4.  Inside that block, call the corresponding engine method (e.g., `engine.executeHoplite(...)`) and return the result in the standard format.

### LOCATIONS
*   **Tools Definition**: Lines 107-168 (The `TOOLS` array)
*   **Handler Logic**: Lines 170-214 (The `CallToolRequestSchema` handler)
*   **Engine Methods**: Lines 183-192 (`helot_slinger`), Lines 195-203 (`helot_run`)

### EVIDENCE
```typescript
// 1. Tools Array Definition (Lines 107-168)
const TOOLS: Tool[] = [
    {
        name: "helot_slinger",
        description: "Delegates deep codebase research or analysis to the local Slinger subagent.",
        inputSchema: {
            type: "object",
            properties: {
                researchTask: { type: "string", description: "..." },
                targetFiles: { type: "array", items: { type: "string" }, description: "..." },
            },
            required: ["researchTask"],
        },
    },
    {
        name: "helot_run",
        description: "Delegates implementation to the Psiloi subagent swarm...",
        inputSchema: {
            type: "object",
            properties: {
                taskSummary: { type: "string", description: "..." },
                implementationPlan: { type: "string", description: "..." },
                tasks: { type: "array", items: { type: "object", ... }, description: "..." },
            },
            required: ["taskSummary"],
        },
    },
];

// 2. Handler Logic (Lines 170-214)
server.setRequestHandler(CallToolRequestSchema, async (request: CallToolRequest) => {
    const { name, arguments: args } = request.params;

    try {
        if (name === "helot_slinger") {
            const researchTask = String(args?.researchTask);
            const targetFiles = Array.isArray(args?.targetFiles) ? (args.targetFiles as string[]) : undefined;

            const result = await engine.executeSlinger(researchTask, targetFiles, (data: { text: string }) => {
                console.error(`[Slinger Update] ${data.text}`);
            });

            return { content: [{ type: "text", text: result }] };
        }

        if (name === "helot_run") {
            ensureWatchOpen();
            const taskSummary = String(args?.taskSummary);
            const implementationPlan = String(args?.implementationPlan ?? '');
            const frontierTasks = Array.isArray(args?.tasks) ? args.tasks as any[] : undefined;

            const result = await engine.executeHelots(taskSummary, implementationPlan, (data) => {
                console.error(`[Helot Update] ${data.text}`);
            }, frontierTasks);

            return { content: [{ type: "text", text: result }] };
        }

        throw new Error(`Unknown tool: ${name}`);
    } catch (error: any) {
        return {
            content: [{ type: "text", text: `Error: ${error.message}` }],
            isError: true,
        };
    }
});
```