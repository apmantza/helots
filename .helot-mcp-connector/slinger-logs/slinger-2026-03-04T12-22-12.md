# Slinger Report
**Task:** In C:/Users/R3LiC/Desktop/helots-pi/src/core/slinger-orchestrator.ts, find:
1. Where FILE_CONTENT_CAP is defined and used
2. The executeSlinger function signature (or whatever the main entry point is called)
3. How targetFiles are preloaded — the loop that reads file content up to the cap

In C:/Users/R3LiC/Desktop/helots-pi/src/core/engine.ts, find:
1. The executeSlinger call — what arguments are passed to it
2. Where serverMaxTokens is available near that call

### SUMMARY
**Findings from Pre-Loaded Files:**

**1. C:/Users/R3LiC/Desktop/helots-pi/src/core/slinger-orchestrator.ts**
*   **FILE_CONTENT_CAP Definition:** Defined as a constant `const FILE_CONTENT_CAP = 25000;` on line 12.
*   **Main Entry Point:** The main function is `async executeSlinger(researchTask: string, targetFiles: string[] | undefined, onUpdate?: (data: any) => void): Promise<string>`.
*   **Target File Preloading Loop:** Located inside `executeSlinger` starting at line 45. It iterates through `targetFiles`, reads each file using `readFileSync`, checks if content exceeds `FILE_CONTENT_CAP`, truncates if necessary, and appends to `slingerHistory`.

**2. C:/Users/R3LiC/Desktop/helots-pi/src/core/engine.ts**
*   **serverMaxTokens Availability:** Defined as a private class member `private serverMaxTokens: number = 0;` on line 117. It is used dynamically within `runOneTask` (around line 330-340) to calculate `builderMaxTokensOverride` for large files.
*   **executeSlinger Call:** The pre-loaded content for `engine.ts` is truncated (ends mid-function in `runOneTask`). The actual call to `executeSlinger` (from `slinger-orchestrator.ts`) is **not present** in the provided pre-loaded content.

### LOCATIONS
*   `C:/Users/R3LiC/Desktop/helots-pi/src/core/slinger-orchestrator.ts:12` - `FILE_CONTENT_CAP` definition
*   `C:/Users/R3LiC/Desktop/helots-pi/src/core/slinger-orchestrator.ts:36` - `executeSlinger` function signature
*   `C:/Users/R3LiC/Desktop/helots-pi/src/core/slinger-orchestrator.ts:45-60` - File preloading loop
*   `C:/Users/R3LiC/Desktop/helots-pi/src/core/engine.ts:117` - `serverMaxTokens` property definition
*   `C:/Users/R3LiC/Desktop/helots-pi/src/core/engine.ts:330-340` - `serverMaxTokens` usage for token budgeting

### EVIDENCE
**slinger-orchestrator.ts:**
```typescript
const FILE_CONTENT_CAP = 25000; // chars per preloaded file — tuned for Q8 KV + 32K context

export class SlingerOrchestrator {
    // ...
    async executeSlinger(researchTask: string, targetFiles: string[] | undefined, onUpdate?: (data: any) => void): Promise<string> {
        // ...
        if (targetFiles && targetFiles.length > 0) {
            // ...
            for (const f of targetFiles) {
                try {
                    const raw = readFileSync(path.resolve(f), "utf-8");
                    const lineCount = raw.split('\n').length;
                    const content = raw.length > FILE_CONTENT_CAP
                        ? raw.slice(0, FILE_CONTENT_CAP) + `\n...[PRELOAD TRUNCATED at ${FILE_CONTENT_CAP} chars — ${lineCount} total lines. Use grep -n / wc -l for accurate counts.]`
                        : raw;
                    // ...
                }
```

**engine.ts:**
```typescript
export class HelotEngine {
    // ...
    private serverMaxTokens: number = 0;
    // ...
    // Usage found in runOneTask:
    // if (!isSurgical && contextContent && this.serverMaxTokens > 0) {
    //   const fileLines = contextContent.split('\n').length;
    //   const estimatedOutputTokens = Math.ceil(fileLines * 15);
    //   const safeMax = this.serverMaxTokens - 4096;
    //   // ...
    // }
```