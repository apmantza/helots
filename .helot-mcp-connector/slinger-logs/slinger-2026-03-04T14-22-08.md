# Slinger Report
**Task:** In src/core/engine.ts, verify these 4 changes were made correctly:
1. The runOneTask function signature (around line 212) has a new last parameter: `writeTrace: (data: any) => void = () => {}`
2. Just before the `const builderRaw = await this.runSubagent(` call, there is a `writeTrace({ phase: 'builder', status: 'start', ... })` call
3. Just after `lastBuilderOut = builderOut.slice(0, 3000)` there is a `writeTrace({ phase: 'builder', status: 'complete', ... })` call
4. After `this.writeEvent({ type: 'verdict', ... result: 'PASS' })` there is a `writeTrace({ phase: 'peltast', status: 'pass', ... })` call
5. After `this.writeEvent({ type: 'verdict', ... result: 'FAIL' })` there is a `writeTrace({ phase: 'peltast', status: 'fail', ... })` call
6. The call to `this.runOneTask(...)` (around line 861) now passes `writeTrace` as the last argument

Show the exact lines for each.

### SUMMARY
All 6 requested changes have been verified as correctly implemented in `src/core/engine.ts`. The `writeTrace` parameter was added to the `runOneTask` signature, and all corresponding trace calls (builder start/complete, peltast pass/fail) and the invocation with the `writeTrace` argument are present at the expected locations.

### LOCATIONS
- Line 222: `runOneTask` signature includes `writeTrace: (data: any) => void = () => {}`
- Line 391: `writeTrace({ phase: 'builder', status: 'start', ... })` call before `runSubagent`
- Line 400: `writeTrace({ phase: 'builder', status: 'complete', ... })` call after `lastBuilderOut` slice
- Line 683: `writeTrace({ phase: 'peltast', status: 'pass', ... })` call after `PASS` verdict
- Line 688: `writeTrace({ phase: 'peltast', status: 'fail', ... })` call after `FAIL` verdict
- Line 861: `this.runOneTask(...)` call passes `writeTrace` as the last argument

### EVIDENCE
```typescript
// Line 222: Function signature
writeTrace: (data: any) => void = () => {}

// Line 391: Builder start trace
writeTrace({ phase: 'builder', status: 'start', taskId: task.id, tryNum: tryCount });

// Line 400: Builder complete trace
writeTrace({ phase: 'builder', status: 'complete', taskId: task.id, tryNum: tryCount });

// Line 683: Peltast pass trace
writeTrace({ phase: 'peltast', status: 'pass', taskId: task.id, tryNum: tryCount });

// Line 688: Peltast fail trace
writeTrace({ phase: 'peltast', status: 'fail', taskId: task.id, tryNum: tryCount, reason: verdictReason.slice(0, 100) });

// Line 861: Invocation with writeTrace
const result = await this.runOneTask(task, runId, modelName, globalContext, implementationPlan, psiloiMetrics, reviewFile, onUpdate, taskNodes, writeTrace);
```