# Slinger Report
**Task:** In src/eba_benchmarking/data/benchmarking.py, show lines 25-35 exactly (the lines just after the config import). I need the exact content including the from .base import line.

### SUMMARY
The requested lines 25-35 from `src/eba_benchmarking/data/benchmarking.py` have been identified from the pre-loaded file content. The lines immediately following the config import (line 24) are the import of `MIN_PERIOD` and `get_master_data` from the `.base` module.

### LOCATIONS
- C:\Users\R3LiC\Desktop\eba-benchmarking\src\eba_benchmarking\data\benchmarking.py:25-26

### EVIDENCE
```python
from ..config import DB_NAME, PROFITABILITY_ITEMS, SOLVENCY_ITEMS

from .base import MIN_PERIOD, get_master_data
```

Lines 25-26 correspond to the requested content:
- Line 25: `from .base import MIN_PERIOD, get_master_data`
- Line 26: (Empty line)
- Lines 27-35: Function definition for `percentileofscore` and its docstring.

Specifically, lines 25-35 are:
```python
from .base import MIN_PERIOD, get_master_data



def percentileofscore(values, score):

    """Delegates to data_utils.percentileofscore. Kept for backward compatibility."""

    from .data_utils import percentileofscore as _impl

    return _impl(values, score)
```