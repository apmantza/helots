# Slinger Report
**Task:** In src/eba_benchmarking/data/benchmarking.py, show the first 40 lines of the get_all_benchmarking_metrics function body (after the def line). Does it call _fetch_profitability_block, or does it still contain inline SQL queries like query_prof? Also show how many @st.cache_data decorators appear immediately before the def line.

### SUMMARY
Based on the pre-loaded file content:

1.  **Function Body Analysis**: The `get_all_benchmarking_metrics` function body (starting after line 67) **does not** call `_fetch_profitability_block`. Instead, it contains **inline SQL queries** (e.g., `query_prof`, `query_solv`, `query_rwa`, etc.) that are executed directly using `pd.read_sql`.
2.  **Decorator Count**: There are **two** `@st.cache_data` decorators immediately preceding the `def get_all_benchmarking_metrics` line.

**Key Findings:**
-   **Inline SQL**: The function defines multiple SQL strings (`query_prof`, `query_solv`, `query_rwa`, `query_cash`, `query_deposits`, `query_wholesale`, `query_aq`, `query_sov`, `query_sov_home`) and executes them directly.
-   **No Helper Call**: It does not use the imported `_fetch_profitability_block` function for the profitability data; it constructs its own query `query_prof`.
-   **Decorators**: The code shows `@st.cache_data` twice before the function definition.

### LOCATIONS
-   `C:\Users\R3LiC\Desktop\eba-benchmarking\src\eba_benchmarking\data\benchmarking.py`: Lines 65-66 (Decorators)
-   `C:\Users\R3LiC\Desktop\eba-benchmarking\src\eba_benchmarking\data\benchmarking.py`: Lines 78-92 (Inline SQL `query_prof`)
-   `C:\Users\R3LiC\Desktop\eba-benchmarking\src\eba_benchmarking\data\benchmarking.py`: Lines 125-138 (Inline SQL `query_solv`)

### EVIDENCE
```python
@st.cache_data
@st.cache_data
def get_all_benchmarking_metrics(lei_list):
    # ...
    query_prof = f"""
    SELECT f.lei, COALESCE(i.short_name, i.commercial_name) as name, f.period, f.item_id, SUM(f.amount) as amount
    FROM facts_oth f
    JOIN institutions i ON f.lei = i.lei
    WHERE f.lei IN ({leis_str}) 
      AND f.item_id IN ({prof_items_str})
      AND f.period >= '{MIN_PERIOD}'
    GROUP BY f.lei, f.period, f.item_id
    """
    
    try:
        df_prof = pd.read_sql(query_prof, conn)
    except Exception:
        df_prof = pd.DataFrame()
```