# Slinger Report
**Task:** In src/core/engine.ts: 1) Find where the function `runOneTask` (defined around line 212) is called from — show the call site with surrounding lines. 2) Show me the exact function signature of runOneTask. 3) Find the exact lines where I should add writeTrace calls inside runOneTask — just before the builder runSubagent call (line ~390), and just after the peltast verdict is determined (line ~672-680). Show those exact lines.

### SUMMARY
1. **Call Site of `runOneTask`**: Found at line 856 in `src/core/engine.ts`. The call is made inside a loop iterating over tasks, passing the current task, runId, modelName, globalContext, implementationPlan, psiloiMetrics, reviewFile, onUpdate, and taskNodes.
2. **Function Signature**: Defined at line 212. It is a private async method named `runOneTask` taking parameters: `task: HelotTask`, `runId: string`, `modelName: string`, `globalContext: string`, `implementationPlan: string`, `psiloiMetrics: { builder: any; peltast: any }`, `reviewFile: string`, `onUpdate: ((data: any) => void) | undefined`, and `allTasks: HelotTask[] = []`. It returns `Promise<{ passed: boolean; escalation?: string }>`.
3. **Insertion Points for `writeTrace`**:
   - **Before `runSubagent`**: Around line 390. The specific line is `const builderRaw = await this.runSubagent(...)`.
   - **After Peltast Verdict**: Around lines 672-680. The verdict logic determines `tier` ('PASS' or 'FAIL') and sets `taskPassed`. The specific location is after the `if (tier === 'PASS')` block and the `else` block handling the FAIL case, just before the loop continues or breaks.

### LOCATIONS
- Call Site: `src/core/engine.ts:856`
- Function Definition: `src/core/engine.ts:212`
- Insertion Point 1 (Before runSubagent): `src/core/engine.ts:390`
- Insertion Point 2 (After Verdict): `src/core/engine.ts:672-680`

### EVIDENCE
```typescript
// Function Signature (Line 212)
private async runOneTask(
  task: HelotTask,
  runId: string,
  modelName: string,
  globalContext: string,
  implementationPlan: string,
  psiloiMetrics: { builder: any; peltast: any },
  reviewFile: string,
  onUpdate: ((data: any) => void) | undefined,
  allTasks: HelotTask[] = []
): Promise<{ passed: boolean; escalation?: string }> {

// Call Site (Line 856)
const result = await this.runOneTask(task, runId, modelName, globalContext, implementationPlan, psiloiMetrics, reviewFile, onUpdate, taskNodes);

// Insertion Point 1 (Line ~390)
const builderRaw = await this.runSubagent(
  "Builder", builder.name, builderSystem,
  `Mission ID: ${runId}\nTask: ${task.description}`,
  onUpdate, psiloiMetrics.builder, builderProfile, modelName, undefined, builderMaxTokensOverride
);

// Insertion Point 2 (Lines ~672-680)
if (tier === 'PASS') {
  taskPassed = true;
  task.status = 'completed';
  this.governor.recordVerification({ passed: true, message: `Task ${task.id} verified` });
  onUpdate?.({ text: `β… PASS β€” Task ${task.id}: ${task.description.slice(0, 70)}` });
  this.writeEvent({ type: 'verdict', taskId: task.id, result: 'PASS', tryNum: tryCount });
  break;
} else {
  onUpdate?.({ text: `β FAIL β€” Task ${task.id} (try ${tryCount}/3): ${verdictReason.slice(0, 120)}` });
  this.writeEvent({ type: 'verdict', taskId: task.id, result: 'FAIL', tryNum: tryCount, reason: verdictReason });
  // Restore backup
  for (const { filePath, fullPath } of filesToProcess) {
```