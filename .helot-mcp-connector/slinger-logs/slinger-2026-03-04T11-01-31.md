# Slinger Report
**Task:** Read src/eba_benchmarking/data/benchmarking.py and report: (1) all imports, (2) all function signatures with their line numbers and approximate line ranges, (3) for each function describe what it does in 1-2 sentences, (4) identify any repeated patterns inside the file (repeated SQL queries, repeated block structures, repeated weighted avg logic, repeated connection open/close). Give exact code snippets for repeated patterns.

### SUMMARY
**File:** `C:\Users\R3LiC\Desktop\eba-benchmarking\src\eba_benchmarking\data\benchmarking.py`

**(1) Imports:**
*   `pandas as pd`
*   `numpy as np`
*   `sqlite3`
*   `streamlit as st`
*   `os`
*   `from ..config import DB_NAME, PROFITABILITY_ITEMS, SOLVENCY_ITEMS`
*   `from .base import MIN_PERIOD, get_master_data`
*   `from .data_utils import percentileofscore` (inside `percentileofscore` function)
*   `from .benchmarking_queries import build_leis_str` (inside `get_all_benchmarking_metrics`)
*   `from .solvency import get_solvency_with_texas_ratio`
*   `from .asset_quality import get_aq_breakdown`
*   `from .assets import get_assets_kpis`
*   `from .liabilities import get_liabilities_kpis, get_deposit_beta`

**(2) Function Signatures & Ranges:**
1.  `percentileofscore(values, score)` (Lines 25-29)
2.  `get_benchmarking_peer_groups(country_iso, region, systemic_importance, size_category=None)` (Lines 32-113)
3.  `get_all_benchmarking_metrics(lei_list)` (Lines 118-858)

**(3) Function Descriptions:**
*   `percentileofscore`: Delegates to `data_utils.percentileofscore` for backward compatibility.
*   `get_benchmarking_peer_groups`: Returns a dictionary of LEI lists for peer groups (Domestic, Regional, EU) based on country, region, and size category, excluding banks without size data.
*   `get_all_benchmarking_metrics`: Fetches and calculates all benchmarking metrics (profitability, solvency, RWA, liquidity, asset quality) for a list of LEIs by querying the SQLite database, merging data, and computing derived ratios.

**(4) Repeated Patterns:**
*   **Repeated SQL Query Structure:** The function `get_all_benchmarking_metrics` repeatedly uses a specific pattern to query `facts_oth` or `facts_cre` tables.
    *   *Pattern:* `SELECT ... FROM facts_... WHERE lei IN ({leis_str}) AND item_id IN (...) AND period >= '{MIN_PERIOD}' GROUP BY ...`
    *   *Example:* `query_prof`, `query_solv`, `query_rwa`, `query_cash`, `query_deposits`, `query_wholesale`, `query_aq`.
*   **Repeated Connection/Exception Handling:** Every SQL query block follows the same `try...except` pattern with `conn.close()` on error or at the end.
    *   *Pattern:*
        ```python
        try:
            df_x = pd.read_sql(query_x, conn)
        except Exception:
            df_x = pd.DataFrame()
        ```
*   **Repeated Merge Logic:** After fetching each dataset, the code attempts to merge it into the main `df` if it's not empty.
    *   *Pattern:*
        ```python
        if not df_x.empty:
            df = pd.merge(df, df_x, on=['lei', 'period'], how='left')
        ```
*   **Repeated Column Initialization:** After merging, columns are initialized to 0 if missing (e.g., `df['cash'] = 0`, `df['customer_deposits'] = 0`).
*   **Repeated `df.apply` for Ratios:** The code uses `df.apply(lambda x: ..., axis=1)` extensively to calculate ratios, often checking for zero denominators.
    *   *Example:* `df['Int Inc / Assets']`, `df['NIM']`, `df['RoA']`, etc.

### LOCATIONS
*   Imports: Lines 13-22
*   `percentileofscore`: Lines 25-29
*   `get_benchmarking_peer_groups`: Lines 32-113
*   `get_all_benchmarking_metrics`: Lines 118-858
*   SQL Pattern (query_prof): Lines 143-153
*   SQL Pattern (query_solv): Lines 173-183
*   SQL Pattern (query_aq): Lines 303-316
*   Merge Pattern: Lines 165-167, 189-191, 215-217, etc.
*   Apply Pattern: Lines 383-390 (Int Inc / Assets), Lines 403-406 (NIM), Lines 433-436 (RoA)

### EVIDENCE
```python
# Repeated SQL Pattern (query_prof)
query_prof = f"""
    SELECT f.lei, COALESCE(i.short_name, i.commercial_name) as name, f.period, f.item_id, SUM(f.amount) as amount
    FROM facts_oth f
    JOIN institutions i ON f.lei = i.lei
    WHERE f.lei IN ({leis_str}) 
      AND f.item_id IN ({prof_items_str})
      AND f.period >= '{MIN_PERIOD}'
    GROUP BY f.lei, f.period, f.item_id
    """

# Repeated Exception Handling
try:
    df_prof = pd.read_sql(query_prof, conn)
except Exception:
    df_prof = pd.DataFrame()

# Repeated Merge Logic
if not df_prof.empty:
    # Pivot profitability data
    df_prof_pivot = df_prof.pivot_table(...)
    ...
    df = pd.merge(df_prof_pivot, df_solv_pivot, on=['lei', 'period'], how='left')

# Repeated df.apply for Ratios
df['Int Inc / Assets'] = df.apply(
    lambda x: (x['Interest Income'] / x['Total Assets']) * x['ann_factor'] if x['Total Assets'] > 0 else 0, axis=1
)

# Repeated Column Initialization
if 'TREA' not in df.columns:
    df['TREA'] = 0
else:
    df['TREA'] = df['TREA'].fillna(0)
```