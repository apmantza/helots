# Slinger Report
**Task:** In C:\Users\R3LiC\Desktop\helots-pi\src\core\engine.ts, search for how "surgical" or "symbol" is tracked for a task during the builder phase. Specifically: (1) what variable name holds whether the current task is surgical (has a symbol), (2) is that variable in scope around line 441 (the content loss check), (3) what is task.symbol called in the task object — is it "symbol", "targetSymbol", or something else? Show the relevant code snippets.

### SUMMARY
Based on the pre-loaded file content and subsequent grep results:

1.  **Variable name for surgical mode:** The variable is `isSurgical` (boolean).
2.  **Scope around line 441:** The variable `isSurgical` is defined in the `runOneTask` method (starting around line 236 in the full file, but the relevant logic block is around lines 240-270). The content loss check (which appears to be the "Ground truth" section where `task.targetSymbol` is checked) occurs later in the same method (around line 430+ based on the grep context). `isSurgical` is defined in the `for` loop scope (or the method scope depending on exact indentation, but it is declared before the loop and used inside it).
    *   *Correction on Scope:* Looking at the code structure, `isSurgical` is declared inside `runOneTask` (line ~238) *before* the `for (let tryCount = 1; ...)` loop. The "Ground truth" section where the symbol check happens is *inside* that loop. Therefore, `isSurgical` **is in scope** at the point where the symbol check occurs (around line 441 in the full file, which corresponds to the grep output showing `if (task.targetSymbol)`).
3.  **Task symbol property name:** The property is called `targetSymbol`.
    *   It is parsed from the checklist line `Symbol: ...` in `parseChecklist` (line ~215).
    *   It is used to determine surgical mode: `if (task.targetSymbol && lang !== 'unknown')` (line ~253).
    *   It is used in the ground truth check: `if (task.targetSymbol) { ... content.includes(task.targetSymbol) ... }` (line ~433 in the grep output).

**Relevant Code Snippets:**

*   **Variable Declaration & Logic (Lines ~238-260):**
    ```typescript
    let contextContent = "";
    let isSurgical = false;
    let surgicalSlice = "";   // the current function body shown to Builder as context
    let lang: LangKind = 'unknown';

    if (task.file) {
      // ...
      if (task.targetSymbol && lang !== 'unknown') {
        const bounds = getSymbolBounds(originalContent, task.targetSymbol, lang);
        if (bounds) {
          isSurgical = true;
          surgicalSlice = bounds.slice;
          // ...
        }
      }
    }
    ```

*   **Task Property Definition (Lines ~210-220):**
    ```typescript
    return {
      // ...
      targetSymbol: (!isCreate && symbolMatch) ? symbolMatch[1].trim().replace(/`/g, '') : undefined,
      // ...
    };
    ```

*   **Symbol Check in Ground Truth (Lines ~430-440):**
    ```typescript
    // --- Ground truth: diff + symbol presence (no tsc available) ---
    const groundTruth: string[] = [];
    // ...
    if (task.targetSymbol) {
      for (const { filePath, content } of filesToProcess) {
        const found = content.includes(task.targetSymbol);
        groundTruth.push(`Symbol check — "${task.targetSymbol}" in ${filePath}: ${found ? '✓ FOUND' : '✗ MISSING'}`);
      }
    }
    ```

### LOCATIONS
*   `C:\Users\R3LiC\Desktop\helots-pi\src\core\engine.ts`: Line ~238 (`let isSurgical = false;`)
*   `C:\Users\R3LiC\Desktop\helots-pi\src\core\engine.ts`: Line ~217 (`targetSymbol: (!isCreate && symbolMatch) ...`)
*   `C:\Users\R3LiC\Desktop\helots-pi\src\core\engine.ts`: Line ~253 (`if (task.targetSymbol && lang !== 'unknown')`)
*   `C:\Users\R3LiC\Desktop\helots-pi\src\core\engine.ts`: Line ~433 (`if (task.targetSymbol) { ... content.includes(task.targetSymbol) ... }`)

### EVIDENCE
```typescript
// From parseChecklist method
const symbolMatch = line.match(/Symbol:\s*([^,\]\)]+)/);
// ...
targetSymbol: (!isCreate && symbolMatch) ? symbolMatch[1].trim().replace(/`/g, '') : undefined,

// From runOneTask method
let isSurgical = false;
// ...
if (task.targetSymbol && lang !== 'unknown') {
  const bounds = getSymbolBounds(originalContent, task.targetSymbol, lang);
  if (bounds) {
    isSurgical = true;
    surgicalSlice = bounds.slice;
    onUpdate?.({ text: `🔬 Surgical mode: targeting \`${task.targetSymbol}\` in ${lang} (${bounds.slice.split('\n').length} lines)` });
  } else {
    onUpdate?.({ text: `⚠️ Symbol "${task.targetSymbol}" not found in ${task.file} — falling back to full-file mode.` });
    task.targetSymbol = undefined;
  }
}

// From Ground Truth section (inside the retry loop)
if (task.targetSymbol) {
  for (const { filePath, content } of filesToProcess) {
    const found = content.includes(task.targetSymbol);
    groundTruth.push(`Symbol check — "${task.targetSymbol}" in ${filePath}: ${found ? '✓ FOUND' : '✗ MISSING'}`);
  }
}
```